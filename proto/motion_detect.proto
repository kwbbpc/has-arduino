// A very simple protocol definition, consisting of only
// one message.

syntax = "proto2";

message MotionDetectMessage {
    optional int32 expectedDistanceCm = 1;
    optional int32 triggeredDistanceCm = 2;
}
